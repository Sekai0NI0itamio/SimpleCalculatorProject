*.pyc
__pycache__/

# Temporary pipeline artifacts (not committed to git)
data/*/chunks/
data/*/version_chunks/
data/*/version_results/
data/*/version_split.json
data/*/version_summary.json
data/*/*.db-wal
data/*/*.db-shm

# Old reports directory (kept for backward compat but no longer used)
reports/
reports_tmp/
version_data_tmp/
